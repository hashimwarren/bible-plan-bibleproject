---
layout: base
title: "Week {{ w.weekNumber }} · BibleProject Plan"
description: "Readings and videos for week {{ w.weekNumber }} of the BibleProject plan."
pagination:
  data: plan.weeks
  size: 1
  alias: w
permalink: "/week/{{ w.weekNumber }}/"
---
{% assign week = w %}
<header class="mb-5 md:mb-6">
  <h1 class="text-2xl md:text-[28px] font-semibold tracking-tight text-slate-900">Week {{ week.weekNumber }}</h1>
  <p class="mt-1 text-slate-600">Overview of readings and videos for the week.</p>
</header>

<div class="grid gap-3 md:grid-cols-2">
  {% for d in week.days %}
  <div class="rounded-lg border border-slate-200 bg-white p-4 shadow-sm" id="day-{{ d.day }}">
    <div class="flex items-center justify-between">
      <div class="font-medium text-slate-900">Day {{ d.day }}{% if d.hasJustice %} <span class=\"text-sm\" title=\"Justice scripture present\">⭐</span>{% endif %}</div>
      <a class="inline-flex items-center rounded-md bg-blue-600 px-2.5 py-1.5 text-white text-xs font-medium hover:bg-blue-500" href="/day/{{ d.day }}/">Go to Day</a>
    </div>
    <div class="mt-3 grid gap-4 sm:grid-cols-2">
      <section>
        <h2 class="text-sm font-semibold text-slate-900">Old Testament</h2>
        {% if d.otReadings.size > 0 %}
          <ul class="mt-1 text-sm text-slate-700 list-disc pl-5 space-y-1">
            {% for ref in d.otReadings %}
              <li>{{ ref }}</li>
            {% endfor %}
          </ul>
        {% else %}
          <p class="text-slate-500">No OT reading.</p>
        {% endif %}
      </section>
      <section>
        <h2 class="text-sm font-semibold text-slate-900">New Testament</h2>
        {% if d.ntReadings.size > 0 %}
          <ul class="mt-1 text-sm text-slate-700 list-disc pl-5 space-y-1">
            {% for ref in d.ntReadings %}
              <li>{{ ref }}</li>
            {% endfor %}
          </ul>
        {% else %}
          <p class="text-slate-500">No NT reading.</p>
        {% endif %}
      </section>
      {% if d.videos and d.videos.size > 0 %}
      <section class="sm:col-span-2">
        <h2 class="text-sm font-semibold text-slate-900">Videos</h2>
        <p class="text-slate-600">🎬 This day has videos. <a class="text-blue-600 hover:text-blue-700 underline underline-offset-2" href="/day/{{ d.day }}/">Open the day</a> to watch.</p>
      </section>
      {% endif %}
    </div>
  </div>
  {% endfor %}
</div>

<div class="mt-6 flex items-center justify-between">
  <div>
    {% if week.weekNumber > 1 %}<a class="inline-flex items-center rounded-md border border-slate-200 bg-white px-3 py-2 text-sm text-slate-700 hover:bg-slate-50" href="/week/{{ week.weekNumber | minus: 1 }}/">← Previous Week</a>{% endif %}
  </div>
  <div>
    {% if week.weekNumber < 53 %}<a class="inline-flex items-center rounded-md border border-slate-200 bg-white px-3 py-2 text-sm text-slate-700 hover:bg-slate-50" href="/week/{{ week.weekNumber | plus: 1 }}/">Next Week →</a>{% endif %}
  </div>
</div>
