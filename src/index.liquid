---
layout: base
permalink: "/"
---
<h1>BibleProject Reading Plans</h1>
<p>Browse days or jump directly by scripture reference.</p>

<section class="search">
  <form id="scripture-form" action="/day/" method="get">
    <label for="scripture">Find a scripture:</label>
    <input id="scripture" name="q" type="text" list="scriptures" placeholder="e.g., James 1 or Genesis 1" autocomplete="off">
    <datalist id="scriptures">
      {% for item in plan.scriptureIndex %}
        <option value="{{ item.ref }}"></option>
      {% endfor %}
    </datalist>
    <button class="btn" type="submit">Go</button>
  </form>
  <script id="scriptureIndex" type="application/json">[
    {% for item in plan.scriptureIndex %}{"ref":"{{ item.ref | escape }}","day":{{ item.day }}}{% if forloop.last != true %},{% endif %}{% endfor %}
  ]</script>
</section>

<h2>All Days</h2>
<div class="grid day-list">
  {% for d in plan.days %}
    <div class="card">
      <div><strong>Day {{ d.day }}</strong> {% if d.videos and d.videos.size > 0 %}<span title="Has video">🎬</span>{% endif %}</div>
      <div class="muted">OT: {% if d.otReadings.size > 0 %}{{ d.otReadings[0] }}{% else %}-{% endif %}</div>
      <div class="muted">NT: {% if d.ntReadings.size > 0 %}{{ d.ntReadings[0] }}{% else %}-{% endif %}</div>
      <div style="margin-top:.5rem"><a class="btn" href="/day/{{ d.day }}/">Open Day</a></div>
    </div>
  {% endfor %}
</div>
